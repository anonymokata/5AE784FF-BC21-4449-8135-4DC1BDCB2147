#include "calculare.h"
#include <string.h>

char buffer[256];

#test int2roman_test
	fail_unless (strcmp ((int2roman (1, buffer), buffer), "I") == 0);
	fail_unless (strcmp ((int2roman (2, buffer), buffer), "II") == 0);
	fail_unless (strcmp ((int2roman (3, buffer), buffer), "III") == 0);
	fail_unless (strcmp ((int2roman (4, buffer), buffer), "IV") == 0);
	fail_unless (strcmp ((int2roman (5, buffer), buffer), "V") == 0);

